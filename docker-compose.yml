# docker-compose.yml (di root project)
services:
  # postgres:
  #   image: postgres:16
  #   environment:
  #     POSTGRES_PASSWORD: postgres
  #     POSTGRES_DB: urlshortener
  #   ports:
  #     - "5432:5432"
  #   volumes:
  #     - postgres_data:/var/lib/postgresql/data

  rabbitmq:
    image: rabbitmq:3-management
    ports:
      - "5672:5672"
      - "15672:15672"

  kafka:
    image: apache/kafka-native:latest
    environment:
      KAFKA_NODE_ID: 1
      KAFKA_PROCESS_ROLES: broker,controller
      KAFKA_LISTENERS: PLAINTEXT://:9092,CONTROLLER://:9093
      KAFKA_ADVERTISED_LISTENERS: PLAINTEXT://localhost:9092
      KAFKA_CONTROLLER_LISTENER_NAMES: CONTROLLER
      KAFKA_CONTROLLER_QUORUM_VOTERS: 1@localhost:9093
      KAFKA_LISTENER_SECURITY_PROTOCOL_MAP: CONTROLLER:PLAINTEXT,PLAINTEXT:PLAINTEXT
      CLUSTER_ID: MkU3OEVBNTcwNTJENDM2Qk
      KAFKA_OFFSETS_TOPIC_REPLICATION_FACTOR: 1   # <- tambahan ini
    ports:
      - "9092:9092"

  # Container KHUSUS buat CLI tools (kafka-topics.sh dkk) — idle terus,
  # tinggal 'docker compose exec kafka-tools ...' kapan aja butuh
  # kafka-tools:
  #   image: apache/kafka:3.8.0
  #   entrypoint: ["tail", "-f", "/dev/null"]
  #   network_mode: "host"

# volumes:
#   postgres_data:
